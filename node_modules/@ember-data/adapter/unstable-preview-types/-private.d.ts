declare module '@ember-data/adapter/-private' {
  export { parseResponseHeaders, determineBodyPromise, serializeQueryParams, fetch, setupFastboot, serializeIntoHash } from "@warp-drive/legacy/adapter/-private";
  
}